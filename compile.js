const path = require('path');
//this is for calculating the right path of the file in the system
const fs = require('fs');
//this is for reading the file and getting its content
const fileToCompile = "BodyMap.sol";